{
  "name": "@open-agent-teams/gateway",
  "version": "0.2.0",
  "description": "API Gateway (Open-Agent-Teams) — 中央编排层，Agent 路由、鉴权、限流、熔断",
  "type": "module",
  "main": "src/api-gateway.ts",
  "scripts": {
    "dev": "tsx watch src/api-gateway.ts",
    "build": "tsc",
    "start": "node dist/api-gateway.js",
    "test": "vitest",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@open-agent-teams/core": "workspace:*",
    "busboy": "^1.6.0",
    "dotenv": "^17.4.2",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/busboy": "^1.5.4",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
